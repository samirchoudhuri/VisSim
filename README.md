# VisSim
The visibility simulator for radio interferometric observations. It generates the visibilities for the point sources and diffuse foreground components. The instrumental noise can also be added in the data.
